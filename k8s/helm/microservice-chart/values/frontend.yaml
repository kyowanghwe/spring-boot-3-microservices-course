# Frontend values
# CI updates ONLY the 'version' field

name: frontend
version: e5fcb8ae4a3493a60e769c7cd8f3e124a8c73d39  # ← CI overwrites this

image:
  repository: kyowanghwe99/frontend
  pullPolicy: IfNotPresent

replicas: 1
port: 80

# Frontend doesn't need heavy resources
resources: null

# No config injection needed for frontend
commonConfigKeys: []
envFromConfigMap: []
